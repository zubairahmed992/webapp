{% extends 'LoveThatFitAdminBundle::base.html.twig' %}
{% block h1 %}Product{% endblock %}

{% block title %}Product{% endblock %}


{% block body %}

<form action="{{ path('admin_product_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}

    <input type="submit" />
</form>

<a href="{{ path('admin_products') }}">back</a>

   <br/><br/>
    
    <form action="{{ path('admin_product_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
    </form>
{% endblock %}


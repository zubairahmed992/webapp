{% extends 'LoveThatFitAdminBundle::base.html.twig' %}
{% block h1 %}Clothing Types{% endblock %}

{% block title %}Clothing Type{% endblock %}


{% block body %}

<form action="{{ path('admin_clothing_type_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
        <input type="submit" />
    </form>

    <a href="{{ path('admin_clothing_types') }}">back</a>
    <br/><br/>
    
    <form action="{{ path('admin_clothing_type_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
    </form>

{% endblock %}


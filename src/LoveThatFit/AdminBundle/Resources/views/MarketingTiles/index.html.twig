{% extends 'LoveThatFitAdminBundle::base.html.twig' %}
{% block breadcrumb %}
  <li class="active">Marketing Tiles </li>
{% endblock %}

{% block body %}
<div class="p_listing">
  <h1>Marketing Tiles </h1>
  <div class="clearfix"></div>
    {% block header_right_links %}
    <div class="adjustment_links" style="margin-top: 20px; float:right;">
      <a href="{{ path('admin_marketing_tiles_new') }}" class="btn">Add New</a>
    </div>
    {% endblock %}
    <div class="clearfix"></div>
    <div class="clearfix"></div>
    <div class="bs-docs-grid">
      <table id="example_marketing_tiles" class="display" cellspacing="0" width="100%">
          <thead>
            <tr>
              <th>ID</th>
              <th>Title</th>
              <th>Created At</th>
              <th>Image</th>
              <th>Status</th>
              <th>Action</th>
            </tr>
          </thead>
          <tbody>
          </tbody>
      </table>
    </div> 
  </div>
</div>

<div id="deleteMyModel" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="myModalLabel">Delete Marketing Tiles</h3>
  </div>
  <div class="modal-body">
      <p>Are you sure you want to Delete this Marketing Tiles?</p>
  </div>
  <div class="modal-footer">
    <button class="btn" data-dismiss="modal" aria-hidden="true">Cancel</button>
    <a href=""  class="btn btn-primary delete_url">Delete</a>
  </div>
</div>

<script type="text/javascript">
  jQuery(document).ready(function() {

    var table = $('#example_marketing_tiles').DataTable({
       "processing": true,
        "serverSide": true,
        "ajax": {
            "url": "{{ path('admin_marketing_tiles_paginate') }}",
            "type": "POST"
        },
        "sAjaxDataProp": "data",
        "pageLength": 10,
        "lengthMenu": [[10, 25, 50, 100], [10, 25, 50, 100]],
        "order": [[ 0, "asc" ]],
        "columns": [
          {
              "data"  : "id",
              "sortable" : false,
          },
          {
              "data"  : "title",
          },
          {
              "data"  : "created_at",
              "sortable" : false,
          },
          {
              "data"  : "image",
              "render": function(data, type, row) {
                    var image_data = '';
                    if(data){
                        image_data = '<img src="'+data+'" style="height:150px !important;" />';
                    }
                    return image_data;
              },
              "sortable" : false,
          },
          {
              "data"  : "disabled",
              "sortable" : false,
          },
          {
              "title"         : "Actions",
              "targets"       : -1,
              "data"          : null,
              "orderable"     : false,
              "searchable"    : false,
              "defaultContent": DetailPageButton +" "+ EditButton+" "+ DeleteButton,
          },
        ]
    });

    jQuery('#example_marketing_tiles tbody').on( 'click', 'button, a, img', function () {
        var dataAction = $(this).attr('data-action');
        var data = table.row( $(this).parents('tr') ).data();
        if (dataAction == 'Edit') {
            var url = '{{ path('admin_marketing_tiles_edit', {'id': ":id"}) }}';
            url     = url.replace(':id', data.id);
            window.top.location = url;
        }
        if (dataAction == 'Detail') {
          var url = '{{ path('admin_marketing_tiles_show', {'id': ":id"}) }}';
          url     = url.replace(':id', data.id);
          window.top.location = url;
        }
        if (dataAction == 'Delete') {
          var url = '{{ path('admin_marketing_tiles_delete', {'id': ":id"}) }}';
          url     = url.replace(':id', data.id);

          $(".delete_url").attr("href", url);
      }
    });
  });

</script>
{% endblock %}
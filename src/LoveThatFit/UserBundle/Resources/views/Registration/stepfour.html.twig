
<h1>

User Registration Step Four
</h1>
<img src="{{ asset(entity.WebPath) }}" height="100" width="100" />

<form action="{{ path('registration_step_four_create', { 'id': entity.id })}}" method="post" {{ form_enctype(form) }}>
     {{ form_errors(form) }}
        
<div>
    {{ form_label(form.file) }}
    {{ form_errors(form.file) }}
    {{ form_widget(form.file) }}
</div>
        
        
         {{ form_rest(form) }}
        
    <input type="submit" value="Next"/>
</form>
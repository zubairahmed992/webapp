saml_metadata:
    pattern:     /saml/metadata
    defaults: { _controller: LoveThatFitOneloginSamlBundle:Saml:metadata }

saml_acs:
    pattern:     /admin/acs
    defaults: { _controller: LoveThatFitOneloginSamlBundle:Saml:assertionConsumerService }

saml_login:
    pattern:     /admin/login
    defaults: { _controller: LoveThatFitOneloginSamlBundle:Saml:login }

saml_logout:
    pattern:     /admin/logout
    defaults: { _controller: LoveThatFitOneloginSamlBundle:Saml:singleLogoutService }

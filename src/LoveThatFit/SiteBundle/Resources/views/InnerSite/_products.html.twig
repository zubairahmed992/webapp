<!--/ this templete should have the objects products, page_number, limit & row_count -->
<ul class="prod_wrapper">

                          {% for p in products %}
    <li>
        <div class="prod_node">
            <span></span>
            <div class="featured_box">                
                <div class="featured_content">
                    <h3>{{p.name}}</h3>
                    <!--<p>{{p.brand.name}} <br/>
                        All sizes Available<br />
                        All colors Available</p>
                    <div class="pro_price">$29.99 - $69.99</div>-->
                </div>
                <div class="featured_img">
                    <img class="prod_{{p.clothingType.target|lower}}" src="{{asset(p.WebPath)}}" width="63" height="108"/>
                    <span class="prod_fitting_details" id="fitting_feedback_{{p.id}}"><a href='{{path("ajax_fitting_feedback_list", {"product_id" : p.id})}}'>feedback</a></span>
                    
                </div>
                    <span class="pro_detail"><img src="{{ asset('bundles/lovethatfit/site/images/slider_detail_arrow.png') }}" width="58" height="39"></span>
                  
                    
                <span></span>
                <!--/prod_node--></div>
    </li>            
                {% endfor %}

</ul>


